@main def mainFunc = 
    {
        // sakta(0)
        // val n = 800 * 4
        // val t1 = System.currentTimeMillis
        // upprepa(n){ upprepa(4){ fram; höger } }
        // val t2 = System.currentTimeMillis
        // println(s"$n kvadratvarv tog ${t2 - t1} millisekunder")

        // sakta(1000)
        // var i = 0
        // while (i < 4) { fram; höger; i = i + 1}

        // for (i <- 1 to 4) { fram; höger; println(s"$i") }

        // (1 to 4).foreach{ i => fram; höger; println(s"$i")}

        // for (i <- 1 to 5) { fram; höger; hoppa; fram; vänster; hoppa; }

        // def kvadrat = for (i <- 1 to 4) { fram; höger }
        // sakta(1000)
        // kvadrat

        // sakta (100)
        // for (i <- 1 to 10) { kvadrat; fram }

        // sakta(100)
        // for (i <- 1 to 10) { for (j <- 1 to 4) { fram; höger}; fram }

        // sakta(500)
        // def kvadrat(sida: Double) = for (i <- 1 to 4) { fram(sida); höger }
        // for (i <- 1 to 3) {kvadrat(30 * i)}

        // ----------- Unicode numbers for W and S
        // sakta(5000)
        // def move(key: Int): Unit = {
        //     println("key: " + key)
        //     if (key == 87) fram(10)
        //     else if (key == 83) fram(-10)
        // }
        // move(87); 
        // move('W'); 
        // move('W')
        // move(83); 
        // move('S');
        // move('S'); 
        // move('S')

        // sakta(0); 
        // activateCanvas()
        // def move(key: Int): Unit = {
        //     println("key: " + key)
        //     if (key == 'W') fram(10)
        //     else if (key == 'S') fram(-10)

        //     if (key == 'A') vänster(5)
        //     else if (key == 'D') höger(5)
        // }
        // onKeyPress(move)
    }